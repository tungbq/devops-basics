apiVersion: v1
kind: Service
metadata:
  name: {{ include "tungleo-chart.fullname" . }}
  labels:
    {{- include "tungleo-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tungleo-chart.selectorLabels" . | nindent 4 }}
