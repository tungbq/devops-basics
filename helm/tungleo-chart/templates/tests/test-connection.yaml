apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tungleo-chart.fullname" . }}-test-connection"
  labels:
    {{- include "tungleo-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tungleo-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
